import styles from "./ApplicationEditLayout.module.scss";
import classNames from "classnames/bind";
import React from "react";

const cx = classNames.bind(styles);

export const ApplicationEdit = () => {
  return <div></div>;
};
